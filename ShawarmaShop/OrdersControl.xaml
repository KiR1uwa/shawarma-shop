<UserControl x:Class="ShawarmaShop.OrdersControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <DataGrid x:Name="OrdersDataGrid"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID"
                                    Binding="{Binding Id}"
                                    Width="60"/>
                <DataGridTextColumn Header="User"
                                    Binding="{Binding ClientName}"
                                    Width="140"/>
                <DataGridTextColumn Header="Placed"
                                    Binding="{Binding CreatedAt, StringFormat=\{0:yyyy-MM-dd HH:mm\}}"
                                    Width="140"/>
                <DataGridTextColumn Header="Ready"
                                    Binding="{Binding DeliveryAt, StringFormat=\{0:HH:mm\}}"
                                    Width="100"/>
                <DataGridTextColumn Header="Items"
                                    Binding="{Binding ItemsSummary}"
                                    Width="*"/>
                <DataGridTextColumn Header="Comment"
                                    Binding="{Binding Comment}"
                                    Width="180"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
